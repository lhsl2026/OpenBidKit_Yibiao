# 商务标第一版使用与验收

本版按当前选择的公司主体输出供人工审核的商务标草稿。入口为易标工具箱的“商务标”。默认主体为隆创信息有限公司，预置贵州云界科创信息技术有限公司，并支持新增公司全称。

## 使用流程

先在页面顶部“公司主体”选择本次投标公司。新增公司时填写完整名称并点击“新增并切换”。切换需确认：招标文件、提取结果、项目名称和截止日保留；原公司的材料、所有表单填写、要求确认和草稿清空，已导出文件保留。切换无需重新提取招标文件，需重新核对要求并导入当前公司材料。公司列表与当前选择在本机 SQLite 工作区持久保存，“开始新项目”保留列表和当前选择。添加名称仅登记选项，不证明公司材料归属。

1. 上传或追加招标文件。解析方式沿用客户端设置，支持查看解析原文；同文件重传可以修复原文缓存。
2. 选择本商务标使用的模型（配置完整的 Codex/DeepSeek 等），提取商务要求。长文件分段处理，五类结果均保留文件、片段和逐字原文依据。失败时仅保留部分结果，不能确认；重新提取会清除旧结果和下游草稿。
3. 核对商务目录、资格要求、废标项、表单字段和商务条款，勾选人工确认。填写项目及商务字段后保存；报价字段禁用，生成时仍强制留空。
4. 导入公司证据库快照，查看候选材料及原件。只有主体归属、核验标记和原件完整性均通过的材料可选，员工逐条勾选并关联资格要求。关键词匹配只推荐候选，不认定满足投标条件。
5. 点击“生成专业商务标”。系统按招标目录、资格审查、商务字段、商务条款和废标风险分包调用当前所选模型，后台生成完整正文并做覆盖审校。完成后预览并分别导出整本 DOCX、待补清单 DOCX；任何影响草稿的输入变化都会使旧草稿失效。

## 公司证据库快照接口

沿用 `integrations/feishu/company-evidence.cjs` 使用的 `records` 台账结构。客户端不依赖集成目录，不访问飞书凭证或生产数据库；从“导入公司证据台账”选择本地 JSON。

- 顶层 `records` 为材料数组。记录保留 `id`、`kind`、`name`、`companyId`、`verified`、`attachments`。
- `kind` 支持 `performance`、`qualification`、`company_certificate`、`certificate`。每条材料编号唯一。
- `companyId` 必须准确等于当前选择的公司全称，`verified` 必须是布尔 true。其他明确主体排除，无归属或未核验材料只显示待核实。生成和导出时再次核对所选材料主体，不跨公司复用。
- 每个附件需要 `relative_path`、`sha256`、布尔 `verified: true`。`relative_path` 相对于所选 JSON 所在目录；从既有附件库导出快照时须一并整理附件路径。只修改名称或勾选客户端不能把待核实材料变成已核实。
- 证书使用 `expires_on: YYYY-MM-DD`，或 `permanent: true/1`。按上海当前日与人工填写的投标截止日较晚者核对有效期。
- 人员 `certificate` 额外要求顶层 `employmentEvidence[人员姓名]`，其中包含相同公司 `companyId`、`verified: true` 和可核验的 `attachments`。没有任职归属原件的人员不能选用。
- 已核验附件会复制到本地商务标工作区，生成和导出时再次检查原文及所选附件哈希；不把企业资料、私人台账、凭证或真实消息写入 Git。

## 本版输出边界

草稿章节由当前招标文件的目录和表单决定，不再固定套用同一组章节。生成器会补充资格审查符合性对照、商务条款逐条响应与偏离确认、证明材料附件索引、无效投标/废标复核和生成覆盖审校，保证每条已提取要求进入正文或审校表。

公司事实以人工填写或经核验后人工选用的台账材料为来源。未确认的材料不会进入正文；缺失内容只在对应位置使用“【待补：具体字段】”。模型负责按章节生成正文，程序校验要求编号并补齐确定性对照表。默认不声明无偏离，不认定政策身份，不自动承诺资格满足；报价、折扣、税率、金额、签字和盖章仍由人工处理。

生成任务失败或应用中断时，上一次成功草稿会保留。长文件拆成多个生成包；相同章节标题会按原顺序合并，避免输出多份重复章节。招标方提供带标签的 DOCX 时仍优先使用“按 Word 模板导出”保留原版式，整本 DOCX 用于补充模板未覆盖的正文和审校表。

本版 DOCX 的资质/人员/业绩章节包含材料索引与核验信息，**不会自动装订 PDF 扫描附件或精确复刻招标原始表单版式**。原件/扫描件装订、格式核对均进入待补清单。报价填写和核算、签字盖章、最终提交必须人工完成。

后台提取在 Main 中持续运行，离开页面后不会取消。处理期间禁止修改工作区；重启后的中断任务标为失败，重新提取。业务状态存 SQLite v25，原文和附件副本位于 `userData/workspace/business-bid`。开始新项目清除业务索引，保留已经导出的文件。

## 可重复验收

在 `client/` 下执行：

```powershell
node --test electron/services/businessBidDomain.test.cjs electron/services/businessBidTask.test.cjs electron/services/aiService.modelSelection.test.cjs
npm run build
npm run smoke:electron-native
.\node_modules\.bin\electron.cmd scripts/business-bid-smoke.cjs
```

页面验收在一个终端启动 Vite，另一个终端启动隔离 Electron：

```powershell
.\node_modules\.bin\vite.cmd --host 127.0.0.1 --port 5173 --strictPort
# 第二个终端，同样位于 client/
$env:BUSINESS_BID_SMOKE_OUTPUT = 'E:\WorkSpaces\OpenBidKit_Yibiao\testfiles\business-bid-v1'
$env:BUSINESS_BID_SMOKE_UI_URL = 'http://127.0.0.1:5173/scripts/fixtures/business-bid-smoke.html'
.\node_modules\.bin\electron.cmd scripts/business-bid-smoke.cjs
```

脚本使用独立临时 userData、真实 SQLite/文件解析/任务服务/preload/IPC/页面/DOCX，模型响应为合成 fixture，不调用真实模型、不发送飞书消息。实际产品全窗口的许可证、设置和侧边栏不是此脚本的验收范围。

页面验收从“标书生成”二级菜单开始，经真实菜单配置和 AppRouter 点击“商务标”进入工作台，避免绕过首页入口漏测。

本机桥接支持多模型后，“本商务标使用模型”会列出桥接启用的 GPT-5/GPT-6 模型并标注推荐项。当前建议 Terra 用于日常整理、Luna 用于轻量任务，复杂任务可选 Astra；模型质量仍需按实际招标原文复核。切换选择会保存到当前工作区并使旧草稿失效，下次要求提取使用新模型。已有分析不会因模型切换自动重跑。

原文引用核对将 PDF 表格转换产生的 `<br>`、`<br/>`、`<br />` 与普通换行等价处理；仅统一换行和空白，不进行模糊匹配，也不忽略文字、数值、标点或中间条件的差异。引用为空、只有换行标记、拼接或改写内容仍拒绝通过。

对于非空但不能连续匹配的摘录，每段最多发起一次单独的原文行号定位。模型仅返回待修正条目的首尾行号，程序从当前片段截取完整原文（保留页码、重复表头和其他插入内容），不修改条目标题或原始文件，再进行同一引用校验。模型无法定位、返回重复/缺失条目或越界行号时仍停止该任务，并显示具体片段及条目，已完成片段保留。定位结果仍需人工核对是否支持条目含义，不能视为自动通过资格审核。

跨页引用修复实测：第二份真实文件由正式任务服务使用 Terra 完成 6/6 段，保存目录 48、资格 81、废标 49、字段 60、商务条款 96 条，共 334 条。客户端重启后只读回查为 success、analysisComplete=true、analysisConfirmed=false，结果保留；人工审核尚未完成。16 项商务聚焦测试通过，新增跨页原文切片、缺失/重复/越界/null 定位拒绝回归。构建在首次内存不足后采用较低 Node 堆与线程配置成功，CJS 语法和 diff 检查通过。私人恢复收据位于 Git 忽略目录 testfiles/business-bid-crosspage/。

### 提取稳定性检查（2026-09-11）

- 重新提取前预检全部原文的存在性、哈希和非空内容，预检失败保留已有要求、人工表单及草稿。正式任务也在模型调用前检查空原文。
- 模型条目必须为对象，标题、引用及可选文本字段不再强制转换类型；畸形返回显示片段、类别及条目位置。模型/引用失败同时注明文件与片段，保留此前 checkpoint。
- 全部片段均未提取到要求时不标记完成；允许封面等单独片段返回空数组。
- 模型发现暂不可用时，下拉框仍显示已保存的任务模型并标注当前列表未返回，避免显示与后台实际选用不一致。
- 新增回归先复现失败后修复；29 项商务及模型聚焦测试、45 项隔离 Electron 页面/SQLite/IPC/DOCX 检查、native smoke 通过。构建首次因 esbuild 系统提交内存不足失败，限制 Node 堆及 Go 构建线程/内存后通过，只有既有 chunk 体积警告。
- 当前真实文件的 334 条引用已通过新逻辑只读回放，未新增真实模型调用。验收数据、日志及只读脚本在 Git 忽略目录 `testfiles/business-bid-audit/`，不代表模型语义或未来所有文件不会出错。

### 公司主体选择验收（2026-09-11）

31 项商务/模型聚焦测试、61 项隔离 Electron 页面与 SQLite/IPC/DOCX 检查、native smoke、构建及 diff 检查通过。覆盖两家预置主体、取消切换、后台处理中禁止切换、同主体不清空、主体与列表持久化、切换保留分析并清空公司相关数据、新主体材料过滤和人员任职核验、旧主体材料在生成/导出时阻断、新主体真实 DOCX 内容及新项目保留选择。合成验收无真实模型调用，收据在 Git 忽略目录 `testfiles/business-bid-company/`。正常客户端重启后只读确认两家公司选项可用，当前主体仍为隆创，原有 334 条结果及 6/6 覆盖保留。

## 2026-09-11 验收记录

- 15/15 聚焦测试通过：归属、附件哈希、人员任职、证书日期与长期证照、同名字段保留、模型结构与引用、长文分段、失败和取消、报价留空、模型选择。
- 隔离 Electron 页面验收 38 项通过，合成模型 fixture 调用 2 次。覆盖新建 v25、v24 升级保留数据、后台互斥、持久化恢复、页面确认及生成、报价禁用、DOCX 导出、内部滚动、重置确认、原文/附件变化阻断、同文件重传恢复。
- `npm run build` 与 native smoke 通过；改动 CJS 语法检查、`git diff --check` 通过。构建只有既有 chunk 体积警告。首次构建曾因系统提交内存不足失败；内存恢复后原命令通过。
- 两份合成 DOCX 已回读正文并通过本机 Office/WPS COM 转 PDF 检查；共 8 页。商务表格启用整行分页、续页重复表头及标题跟随正文。工具要求的 LibreOffice 本机不可用，因此采用本机 Word 兼容渲染接口。
- 独立代码审查的同名字段丢失、模型结构过宽、长期证照类型、日期时区及重复导入恢复问题均已修复并增加回归验证。
- 合成输出与截图保存在 Git 忽略的 `testfiles/business-bid-v1/`；它们仅用于功能验收，不代表真实公司投标文件已完成。未调用真实招标文件模型，真实招标格式和材料装订仍需员工实际项目复核。
- 入口问题修复：移除商务标菜单遗留的开发中 notice。新增首页点击回归先复现拦截失败，修复后隔离 Electron 39 项全部通过，`npm run build` 通过；本次收据在 `testfiles/business-bid-entry/验收结果.json`。
- 多模型升级：桥接允许列表、CLI 模型传递、JSON/SSE 模型名和分模型缓存已验证；GPT-5.6 Terra/Luna 各一次真实合成请求完成，未发送真实公司资料。隔离页面验收扩展到 43 项，覆盖 GPT-5/GPT-6 选项、切换持久化与后台任务传递；构建、53 项桥接聚焦检查和客户端模型/商务测试通过。发现并修复 Luna 被中转站兼容规则全局替换为 Terra 的问题，规则现仅对金龙生效。本机桥接与客户端已重启，商务标当前模型为 Terra，生产只读预检 7/7 通过。收据在 `testfiles/business-bid-models/`，早期 Luna 收据已标记无效，以 `live-luna-confirmed.json` 为实际 Luna 调用记录。
- PDF 表格引用修复：真实文件第一段因 `<br>` 与换行差异导致 15 条引用误判，旧缓存 46 条回放全部通过；第二段继续通过正式任务服务调用 Terra，最终 2/2 段完成，目录 29、资格 23、废标 27、字段 18、条款 27 条落库。未改原文或跳过引用校验，人工确认保持 false；这验证了该文件的要求提取，不代表人工审核或整本投标文件完成。新增换行兼容及改数值/省略条件/空引用拒绝回归，14 项商务测试和构建通过。私人恢复收据：`testfiles/business-bid-models/extraction-recovery-result.json`。
