# 飞书联动本机验收记录

首次验收：2026-09-11；最近更新：2026-09-23。公司主体：隆创信息有限公司。范围包括本机易标、指定测试群和已切流的内部正式群，保留员工决策、目录及事实确认；不包含自动报价、签章或投标提交。

## 已验证能力

| 环节 | 实际证据 |
| --- | --- |
| 雷达接收 | 授权用户身份轮询来源群，专用机器人接收卡片回调；来源群、发送者、目标群和操作人员使用白名单。真实五项目标讯中两项重点预读，三项保留员工选择。 |
| 正文取得 | 从贵州官方 XML-ZYZF 取得 125 页、149 页 PDF，记录来源、长度和 SHA-256；原补件动作已完成，修复过程复用同一 document/version，未重传。 |
| Codex 模型 | 官方 Codex CLI 的现有 ChatGPT 登录，gpt-6-astra，HTTP Responses；只读、禁用工具、独立临时目录。真实 JSON、文本和 SSE 兼容验收通过，没有提取登录令牌或要求额外厂商 Key。 |
| 生成 Word | 真实 Codex 和隔离 Electron 完成合成两节技术方案，经章节范围、目录和事实确认后生成有效 DOCX，实际发送测试群并回读。明确标记“合成验收／非真实投标”，不等于真实公司投标文件。 |
| 医院真实预读 | 兼容回放复用已有 12 份提取缓存，193/193 条保留；桥接请求数 22→22，无新增模型调用。原文版本和校验和不变，任务提取完成。报告含 12 条资格、18 条红线、31 条合同、3 条符合性、14 条交付服务要求。 |
| 医院群内查看 | 报告交接快照更新至 r2、58 条可引用要求，原判标卡更新并回读。建议仍为“暂缓／待核实”；21 条未明确分类或分值的评分条目保留人工复核，未猜测评分闭合或公司资格满足。 |
| LED 真实预读 | 原 149 页正文已解析，19 份缓存全部兼容。完整 inline 回放恢复 261 条落库记录并更新报告与交接快照 r2；与离线 262 条的差异来自既有基础字段规范化，评分条目未减少。评分分类缺失仍为 partial／部分结果／待复核；41→41 无新增模型调用。 |
| 飞书文档归档 | 已创建公司专用归档目录，两份报告均已导入为飞书在线文档，医院约 3 万字、LED 约 4.6 万字；全文回读分别 269、272 条内容行无缺失。用户补授权后，同一身份读回确认指定测试群拥有 view 权限；两个任务自动转 published，原项目卡报告链接更新并回读成功。 |
| 一眼判标摘要 | 两张原项目卡已改为资格结论、商务判断、一票否决统计、核心参数、前三风险和三项行动，不再展示分页原文；消息回读确认六部分齐全且无旧分页。两份在线文档原“投标决策摘要”已原位替换为相同口径，冗长判断依据删除，后续正文和全部原章节保留；两文档回读 revision 7。后续新归档报告自动先写摘要，再保留原报告为详细证据附录。 |
| 公司证据 | 既有台账 1,638 条；四条业绩按原件确认归属，异名公司排除，其他资料继续待核实。 |
| 其他标书入口与模型选择 | 每张判标卡保留“生成其他标书”，正式群另投放长期入口卡；按钮均为纯跳转，Windows 飞书使用 `yibiao` 协议唤起本机易标，其他端回退 `https://yibiao.pro`，不依赖卡片回调。长期入口明确区分技术标和商务标，存量无类型链接进入标书生成选择页。易标上传页列出已配置文本模型，Codex `gpt-6-astra` 为推荐项，同时保留 DeepSeek 和其他完整配置；选择结果持久绑定当前标书，并传入普通 AI 与 Pi Agent 链路。无预读报告也可直接上传招标文件。 |
| 真实写标全流程 | 使用余庆县人民医院 125 页官方 PDF 和当前 r4 handoff 建立独立验收任务，源文件 SHA-256 为 `ac2fc5f8a1ec9f473132c7cea269693fda42017fbe5be2d1fbff078441469cfc`。选取两个代表性一级章节，展开 48 个叶子小节；48/48 正文成功，一致性检查完成且无冲突，保留全部未知事实占位。生成有效 DOCX（44,871 字节，SHA-256 `4c578900122f34aa650eb18830ff9fc19deb872d5f9faaee6d5e8133baa5a68d`），OpenXML 主文档、54 个标题段和验收标识均通过检查。文件由专用机器人发送指定测试群，并以 bot 身份回读到同一条 file 消息。原医院和 LED 项目的 `human_decision` 仍为空。 |

## 关键实现

- 本机桥接仅监听回环地址、独立鉴权，Docker Desktop 实测可通过 host.docker.internal 访问。串行模型队列、输出限长、超时取消、24 小时持久结果缓存；失败及结果不明不自动重复调用。
- 编写尝试绑定任务与人工重试次数；重复回调不能启动新的调用。保留原确认流程、事实占位、正文检查和 Word 导出，Codex 路径使用纯文本技术方案。
- 预读兼容补丁接受数字页码字符串，原样保留多页范围并降低置信度；未知强制性、可补齐性、评分分类保持未知，不使用 false 或任意分类代替。
- 缓存回放入口复用原子报告落库，并捕获新的交接快照。同一正文的报告更正形成新报告版本，旧卡确认与旧稿投递被阻断。
- 卡片把预读内部字段转换为中文，识别有证据的中文截止日期；冲突、非法日期和未确认日期均保持待核实，更正后的截止时间优先于旧值。
- 雷达游标、来源编辑隔离、正文获取阶段、卡片更新和附件投递均有持久状态；执行结果不明时保留人工核对，不能重复上传已解决的补件动作。
- 在线报告按来源版本、SHA-256、报告 ID、报告修订号和内容哈希归档；旧版保留，报告修订后撤销旧卡片链接。导入状态与票据持久化，未知结果不重复创建。仅授予指定群阅读权限，不开放互联网链接分享。
- 决策摘要以判标结果为唯一依据，不新增模型调用。只从基础/合同字段提取商务参数，禁止把废标条款中的“超预算”“保证金”等文字误当成项目金额；说明性条款不会挤入前三风险。“待核验”始终不等于满足。
- 写标 Worker 在 Electron 超时或重启后读取易标持久化任务：普通 `paused` 状态调用正式 `resume` 入口，保留已成功正文；`error/paused + awaiting_content_decision` 均转换为可审计的失败小节确认。模型失败缓存不会自动重跑，只有显式确认的重试使用新的 attempt。真实验收曾从 46/48 断点恢复，随后完成 48/48。
- 易标注册 `yibiao` Windows 协议并使用单实例处理首次启动和二次唤起；深链接只接受 `yibiao://new-bid`，聚焦已有窗口并切入技术标工作台，不静默清空已有工作区。任务模型只保存 provider、模型名和显示标签，不复制密钥；执行时从当前已配置 provider 取得连接参数，并以任务模型覆盖全局活动模型。

## 验证与部署

消费者补丁在独立 checkout 构建，旧飞书平台未提交文件未修改。完整依赖安装曾使 Docker Desktop 中断；随后恢复 Docker，使用已有 Linux 镜像依赖加独立编译的 dist 构建派生镜像，原镜像保留。补丁、基线、哈希和复现脚本见 `integrations/feishu/deployment/preread-compat/`；future producer prompt patch 未部署，确保缓存请求保持一致。

消费者最终 5 套聚焦测试 207/207 通过，Nest 构建与补丁反向应用校验通过；实际缓存回放和群内回读独立完成。Task 3 收口后的 OpenBid 完整套件 248 项：247 通过、0 失败、1 项因缺少本地公开包跳过，包含真实隔离 Electron、断点恢复和 DOCX 导出。客户端 `tsc --noEmit && vite build` 通过；首次打包因 Windows 提交内存不足退出，临时停止 Docker 并限制 esbuild 并发后完整构建成功，随后 Docker、全部预读容器和主服务健康均已恢复。生产 Worker 的单阶段上限仍为 30 分钟。

Task 4 收口验证扩展为 259 项：258 通过、0 失败、1 项因缺少本地公开包跳过。新增用例覆盖生产/测试群隔离、显式切流、production 卡片与 DOCX 投递、报告归档、运行 readiness、正式群/成员/来源群/归档目录只读诊断以及外部群拒绝。客户端再次完成 `tsc --noEmit && vite build`；首次构建仍因 Windows 提交内存不足失败，临时停止 Docker Desktop 后成功，随后恢复 9 个容器，其中有健康检查的 8 个均为 healthy。切流后主服务 `/ready` 返回 production/已配置/无缺项，生产预检 7/7 通过。

主运行目录由 `\OpenBidKitFeishu` 开机任务监管，使用现有 PowerShell 7 RemoteSigned 配置，未修改系统执行策略。主服务 `127.0.0.1:4381`、桥接 `127.0.0.1:4383` 实测 ready 200。任务不再依赖交互登录；关机、休眠及 Docker Desktop 未能在后台恢复时不能接收。

2026-09-11 已建立独立内部群“隆创投标决策群”，当前用户为群主，专用判标应用已入群；正式群 ID 只写入忽略的本机 `.env`，与测试群无重叠。Windows 任务已由账户持有人通过系统凭证窗口升级为 `Password + AtStartup`；受控结束旧交互实例后，由新任务真实启动并恢复 Docker、Codex、卡片长连接和主服务。密码只交给 Windows 任务计划程序，未进入命令行、`.env`、日志、Git 或验收记录。

生产预检 7/7 通过后，已原子切换 `BID_DELIVERY_MODE=production` 与 `BID_PRODUCTION_CUTOVER=true`。服务 `/ready` 回读 `mode=production`、`delivery.target=production`、`configured=true`、无缺项。切流时发送一条无交互的 Card 2.0 上线验证卡，内容明确资格不足仍待核实，投标决定、报价、签章和提交由员工确认；发送后以同一 bot 身份回读到相同 interactive 消息并保存私有收据。切流完成，仍需在以后一次真实主机重启或无人登录窗口复核 Docker Desktop 的系统启动行为；关机和休眠期间不能接收消息。

2026-09-11，桌面端生产构建通过；本机配置中的 Codex 桥已更新并保留 DeepSeek 为全局默认，模型列表实测包含推荐的 Codex `gpt-6-astra`、DeepSeek 和其他已配置模型。最小真实请求取得 Codex 响应。Electron 窗口启动后已注册协议，执行 `yibiao://new-bid` 时进程数量保持稳定，证明复用单实例。长期“生成其他标书”Card 2.0 已由专用机器人发送正式群并以 bot 身份回读，卡片内容和 interactive 类型一致；当前 3 张存量判标卡也已复用原消息刷新，bot 回读均确认包含“生成其他标书”。消息、群和凭证收据仅存在忽略目录。

本阶段完整飞书集成套件 261 项：260 通过、0 失败、1 项因缺少本地公开包跳过；客户端服务套件 16/16 通过，`tsc --noEmit && vite build` 通过。完整套件结束后独立复跑生产预检 7/7 通过，主服务 `/ready` 为 production、投递配置完整、无缺项。最终易标窗口仅有一个主进程和一个主窗口，协议二次唤起未创建第二个主实例。

真实写标验收在 30 分钟单阶段超时后保留 46/48 正文，通过持久化 `resume` 恢复另一个中断小节；最后一个模型失败小节经独立、显式 attempt 重试成功。最终 SQLite 任务为 success/100%，48 个小节全部 success；DOCX 和飞书文件回读独立复核通过。验收稿标题和正文均标注“真实文件流程验收稿，不代表公司决定投标”，不构成公司投标决定或完整投标文件。

## 仍需业务人员确认

2026-09-10 16:09，用户发布包含回调订阅的新应用版本后，长连接收到 2 条 card.action.trigger，丢弃 0 条。运行库记录 2 次 view_updated，对应 LED 原卡翻至第 3/12 页；更新队列已送达，飞书消息回读页码一致，用户确认正常翻页。两项目 human_decision 仍为空。回调链路验收完成；医院卡本次未取得独立翻页记录，不将其写成单卡验收成功。

此前截图中的订阅方式和回调项已正确，但变更尚需发布生效。官方[添加回调](https://open.feishu.cn/document/event-subscription-guide/callback-subscription/add-callback.md)第 6 步明确要求创建版本、申请线上发布并审核通过；仅有监听器 ready 或保存订阅不能替代真实验收。

公司证明材料、人员资格、评分归类及来源核验不足时继续“待核实”；助手不代替员工确认投标，不自动报价、签章或提交。

私人收据、文件、真实消息和文档 ID、数据库及凭证只保存在忽略目录，不进入 Git。操作说明见 [集成 README](../integrations/feishu/README.md) 和 [Windows 部署](../integrations/feishu/deployment/README.md)。

## 2026-09-15 标书入口升级

- 商务标少填表、Word 模板、多公司主体和任务级多模型增强已收口为提交 `b219c71`；54 项商务定向测试、49 项飞书桥接定向测试、Electron native smoke、生产构建、20 项效率验收和 64 项隔离 Electron 页面/IPC/SQLite/DOCX 验收通过。
- 深链接新增技术标和商务标专用目标；存量 `yibiao://new-bid` 改为打开标书生成选择页。首次启动事件增加 Renderer 就绪握手，避免 Main 在 React 订阅前发送导致链接丢失。实际 Electron 验证首次商务标、运行中选择页和运行中技术标三条路径均进入对应页面。
- 正式群原长期入口消息已原位更新为“生成技术标”和“生成商务标”两个按钮，没有发送重复卡片。机器人身份回读确认消息已更新且两个按钮可见；飞书消息回读只展开 Web fallback，不返回 `pc_url`，专用 `pc_url` 由卡片结构测试验证，仍需以一次用户真实点击作为客户端侧最终验收。
- Windows `yibiao` 协议登记仍指向当前客户端；更新后生产预检 7/7 通过，主服务保持 production ready。代码提交 `7a762b4` 并已推送至 `origin/codex/feishu-integration`。

## 2026-09-15 Electron 任务订阅稳定性

- 定位并修复同一渲染窗口反复调用任务 IPC 时重复绑定 `WebContents.destroyed` 监听器的问题。任务订阅集合原本已去重，但生命周期监听没有去重，连续调用会触发 `MaxListenersExceededWarning`；现在只在首次订阅时绑定一次，重复订阅仍保留活动任务快照回放。
- 新增回归测试，修复前连续 12 次订阅稳定复现 12 个监听器和 Node 警告，修复后只保留 1 个监听器。同步把模型持久化测试中的数据库版本断言更新为当前 schema v25，避免把既有商务标迁移误判为回归。
- Electron 运行时依次验证首次商务标、运行中选择页、运行中技术标，并额外连续执行 20 次 `getActiveTasks`；页面路由均正确，主进程未再输出监听器警告。客户端 Electron 运行时服务测试 66/66、飞书集成 265 项（264 通过、1 项按设计跳过）、Electron native smoke 和生产构建通过；生产只读预检 7/7 通过。

## 2026-09-15 预读选择卡可观测性

- 排查“勾选预读项目后无反应”：现存选择批次仍停在 `waiting`，没有活动回调事件，也没有形成重复预读任务。选择卡本身是两步表单，勾选下拉项只设置表单值，必须再点击蓝色“预读所选项目”才会提交。
- 所有仍在等待的原卡已原位更新为明确的“第 1 步勾选、第 2 步点击提交”说明，机器人回读 3/3 均包含新提示，没有创建重复消息。以后版本启动时会按内容差异幂等刷新等待卡。
- `/ready` 新增安全的 `cardCallback` 运行信息，只公开 enabled/ready、接收与拒绝计数、固定错误码和最近时间，不包含群、人员、消息、回调值或凭证。部署后回读为 enabled/ready，初始 accepted/rejected 均为 0；用于下一次真实提交后精确判断事件是否送达和是否通过校验。
- 新增两项回归，完整飞书集成套件 266 项：265 通过、0 失败、1 项按设计跳过；生产只读预检 7/7 通过。

## 2026-09-15 正式群选择回调验收与历史批次恢复

- 飞书后台原先把卡片回调发送到已退役的 HTTP 地址，新 OpenBidKit 服务使用长连接，因此早期点击未进入当前监听器。后台改为长连接、保存 `card.action.trigger` 并发布应用 1.0.3 后，用户真实点击已到达新监听层。
- 首次真实提交暴露第二个问题：预读 API/Worker 未注入正式选择群和操作人白名单。补齐部署环境、重建两个容器后，健康检查恢复；本次失败没有创建重复预读任务。
- 当前两项目选择卡来自切流前的历史批次。远端两条事件仍绑定旧群，且已被旧流程置为 `declined`；本地则保留等待卡。恢复前逐条确认来源消息一致、事件类型为项目、无关联任务，唯一不满足项为目标群。备份后仅迁移当前两条事件到正式群并恢复等待状态，再重放用户已经提交的原事件：所选 1 项创建任务，未选 1 项保持关闭，本地卡片转为 `selected` 并已送达。
- 所选项目来自贵州招标投标公共服务平台。官网本次只公开公告 PDF，没有公开完整招标文件，因此任务正确停在 `waiting_upload`；现有规则不会用公告代替完整招标文件形成可投结论。取得完整文件后需从易标入口上传，任务才能继续解析、生成报告并归档飞书文档。
- 临时前台监管器已受控停止，服务由 `\OpenBidKitFeishu` 开机计划任务重新接管。计划任务为 Running，`/ready` 返回 production、长连接 ready、无缺项，生产只读预检再次 7/7 通过。

## 2026-09-16 黔西南云桌面真实文件验收

- 正式群接收 `黔西南云桌面招标文件.pdf` 后，将其识别为“黔西南州检察工作网及云桌面升级改造技术服务采购项目（二次）”，与此前项目隔离处理。111 页正文完成 22/22 分片提取，0 分片失败，共形成 339 条证据信息。
- 首次 handoff 缺少报告交接快照，重新生成报告后接口恢复，交接状态为 `needs_manual`。该状态在 OpenBidKit 中保持“人工复核”，没有形成公司可投结论，也没有代替员工做投标决定。
- 生产排查确认 Worker 的旧 90 秒分片超时短于 Codex 桥接实际调用窗口，同时模型输出存在结构不稳定。预读服务现使用 280 秒分片超时、30 分钟批次超时，并强化抽取提示词：固定顶层数组、数值置信度和合法页码。真实 22 个分片随后全部成功；模型服务、预读 API 和 Worker 健康。
- OpenBidKit 已创建独立项目并向正式群发送项目卡。报告归档任务依次完成导入、群权限授权和验证，最终为 `published`；项目记录已绑定飞书在线文档链接，卡片更新队列投递成功。
- 使用生产归档身份完成三项真实回读：在线文档正文可读，约 2.3 万字符且包含项目标题和投标决策摘要；正式群拥有直接 view 权限；群内项目卡可读，并包含同一报告入口和项目标题。
- 主服务 `/ready` 返回 production、已配置、无缺项；生产只读预检 7/7 通过。底层预读 API、Worker、模型服务、PostgreSQL、Redis、MinIO 等服务均在运行，其中配置健康检查的服务均为 healthy。私人文件、消息、群、任务、文档 ID 和本机收据未进入 Git。

## 2026-09-16 正式群成员文件入口验收

- 定位群成员发送 PDF 后无响应的真实根因：飞书 CLI 在线消息把附件描述返回为 `<file key="..." name="..."/>`，原入口只识别 JSON 和旧 Markdown。解析器已兼容该 XML 形态，并保留消息、发送者、扩展名、文件头、大小和目标群校验。
- 对正式群中一份既有完整 PDF 做精确单消息重放，文件经过状态卡、下载校验、上传和预读任务创建。首次事实提取停在部分结果后，只调用已有的显式恢复入口完成 30/30 分片、259 条证据；报告使用确定性重建入口生成，没有对结果不明的模型请求做自动重跑。
- OpenBidKit 创建独立判标项目并投递项目卡，群文件状态卡原位更新为完成。报告归档任务达到 `published`；生产身份回读确认正文非空、开头包含投标决策摘要、一眼判断、一票否决、核心商务信息、主要风险和下一步，正式群拥有 view 权限，最新项目卡指向同一报告。
- 该项目 handoff 保持 `needs_manual`。现有隆创信息有限公司资料只把已核验归属证据用于判断，证据不足项继续显示待核实，没有形成“可投”结论，也没有自动报价、签章或提交投标。
- 同一原消息在服务重启前后分别重放。两次均直接返回既有完成任务；群文件任务、项目、判标卡队列、报告归档、预读任务、事实提取任务和分片数量全部不变，未产生重复模型处理。
- `OpenBidKitFeishu` 由任务计划程序完成受控重启，继续使用 `Password + AtStartup`；批处理登录会话不向交互终端公开进程命令行时，仓库停止脚本会按安全设计拒绝操作，已在部署手册记录精确计划任务重启流程。旧 `PrereadLarkAgentDaemon` 保持禁用，旧预读计算底座保持运行。重启后 `/ready` 返回 production、无缺项，生产只读预检 8/8 通过，报告和卡片再次回读通过。
- 最终完整飞书集成套件 293 项：292 通过、0 失败、1 项按环境跳过；XML 在线附件格式已有回归覆盖。
- 本轮私人群/消息/任务/文档标识、报告正文和操作收据只保存在 Git 忽略目录，没有写入仓库或跨项目记忆。

## 2026-09-23 统一项目卡与正式群切换验收

- 新卡动作已统一到 `selection.*`、`preread.*`、`company_match.*`、`writing.*` 四个命名空间；回调入口继续兼容旧 `agent + action` 和旧选择表单。选择卡提交后原位显示“已转入预读”，移除操作按钮；已完成旧卡即使换用新的回调事件 ID 重放，也只返回终态结果，不会再次启动预读。
- 旧群已加入硬禁发列表。活动、测试、正式目标或其白名单只要与禁发列表交叉，配置即以 `forbidden_chat_target` 拒绝启动。正式运行手册使用“隆创投标决策群”，未恢复妙搭链路，也未启动第二个 OpenBidKit 或预读 Worker。
- 测试切换前已备份两个运行环境文件，确认生产 `/health`、`/ready` 正常，卡片长连接 ready，所有待投递、文件、群状态、重绑、收件箱、写标和活动群文件队列均为 0。OpenBidKit 只通过现有计划任务受控结束和启动；预读 Worker 的容器 ID 与启动时间在整个验收期间保持不变。
- 首次精确重放暴露真实配置缺口：OpenBidKit 已正确切到测试群，但预读 API 的选择群白名单仍只有正式群，上游明确返回 `chat_not_allowed`，没有创建任务或报告。临时把固定测试群加入同一预读 API 白名单并仅重建现有 `preread-api` 后，API 恢复 healthy；第二个同哈希样本被首个失败任务按本地 canonical 规则安全终止，也没有触发上游或模型调用。最终改用另一个已在预读端存在、可按原消息精确定位的历史测试样本完成验收，避免收费模型重跑。
- 成功样本完成消息文件获取、格式与大小校验、对象存储、上游幂等任务复用、状态卡原位更新和统一项目卡创建。五模块链路回读为 `preprocess`、`decompose`、`response_matrix`、`gap_analysis` 全部 completed，知识报告与 handoff 已存在；状态卡显示“已完成”，项目卡包含报告入口。
- 项目卡保持人工复核门禁：资格结论为暂缓核实，43 项待核验，显示“等待员工判断”，写标入口禁用。用户在测试群真实点击“暂缓”后，长连接 `accepted=1`、`rejected=0`，项目持久决策为 `defer`；原活动 `messageId` 未变，卡片原位回写为“暂缓”，写标任务仍为 0，因此没有替代员工确认跟进、报价、签章或提交。
- 对同一原文件消息再次精确重放，直接返回同一个 completed 群文件任务。重放前后本地仍为 1 个来源任务、1 个当前项目和 1 个活动卡流；预读端仍为 1 个任务、1 份报告、1 个 tender run 和原 4 条模块运行记录，待投递队列保持 0，没有第二活动卡、第二预读任务、第二报告或新增模型处理。
- 验收后已恢复 `BID_DELIVERY_MODE=production`、`BID_PRODUCTION_CUTOVER=true`，预读 API 白名单恢复为仅正式群；只重建 `preread-api`，单一 Worker 未重启。主服务最终 `/health` 正常，`/ready` 回读 production、正式目标已配置、卡片长连接 ready、无缺项；生产预检 8/8 通过，全部待处理队列再次为 0。
- 三群最终回读：正式群最新消息仍是当日 06:19 的统一机器人交互卡，测试群最新消息是本轮 10:27 的统一项目卡；禁发旧群最新消息仍停留在 9 月 17 日且只有预读机器人，证明本轮没有向旧群发送测试卡、报告或文件。正式和测试群均包含两个预期 App ID，禁发旧群不包含统一机器人。
- 应用基础名称已在开发者后台保存为“联合智能投标助手”，页面标题回读一致；预读应用名称保持不变。群成员接口在正式和测试群仍返回统一 App ID 的旧显示标签“投标智能体自建运行服务”，说明群侧显示缓存或独立机器人展示配置尚未收敛。未擅自发布新版本或猜测机器人配置；若缓存仍不刷新，需要业务人员在开发者后台只读核对机器人展示名和生效版本，再决定是否发布。
- 首次全量运行时，真实临时 Node 消费者用例在 Windows 全套高负载下超过测试夹具的 1 秒启动上限；该用例单独运行及与写标测试并发运行均在约 0.17 秒通过。测试夹具仅为这一条真实进程用例将启动窗口放宽到 5 秒、断言窗口放宽到 7 秒，生产默认 30 秒和生产代码均未改变；随后定向与完整复测通过。
- 最终只读代码审查发现并修复三个阻断：启动刷新和非项目卡投递现在都会拒绝已迁出或禁发群的历史选择卡；已签名 HTTP 回调与长连接共用命名空间规范化和分发，同时保留旧 `agent + action`；非字符串或继承属性形式的畸形动作会被拒绝且不打断后续事件。四条新增回归先失败后通过；旧 HTTP 集成夹具同步使用真实 `om_` 消息 ID。最终完整飞书集成套件共 361 项：360 通过、0 失败、1 项按环境跳过，差异检查通过。
- 上述修复通过停止并启动同一 `OpenBidKitFeishu` 计划任务加载，未重建预读 API、未重启预读 Worker、未切换测试模式。加载后 `/health` 与 `/ready` 均为 HTTP 200，production 投递已配置、卡片长连接 ready、无缺项；生产预检 8/8，七类活动投递/工作队列均为 0。
- 回滚步骤：结束 `OpenBidKitFeishu` 当前计划任务实例；把主服务恢复为 production/cutover true；把预读选择群白名单恢复为仅正式群；仅用现有 compose 项目重建 `preread-api`；重新启动同一计划任务；最后依次验证 `/health`、`/ready`、生产预检、队列和三群最近消息。任何一步失败时保持旧群禁发、不开第二 Worker、不自动重放模型请求。
