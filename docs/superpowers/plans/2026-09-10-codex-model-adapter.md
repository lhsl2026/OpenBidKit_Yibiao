# Codex 模型适配执行计划

用户明确选择当前登录的 Codex，并要求接好。沿用已确认的群内预读、证据核验和人工确认设计，给现有 OpenAI-compatible 调用增加本机执行适配，不要求另外提供厂商 API Key。

- [x] 验证官方 CLI 已通过 ChatGPT 登录；以 gpt-6-astra、HTTP-only provider、禁用工具的只读调用返回真实探针结果。
- [x] 验证 Docker 可经 host.docker.internal 访问主机 127.0.0.1；无需公开服务端口。
- [x] Executor：独立临时目录、固定响应 schema、stdin 消息、输入/输出限长、超时和取消、禁用工具、无内部自动重试。
- [x] Bridge：本机 Bearer 鉴权、文本 chat completions 与 SSE、JSON 对象校验、串行队列、持久幂等和未知执行结果隔离。
- [x] 主进程生命周期、就绪状态与写作配置接入；原 API 配置保留可选。
- [x] 仅用 Compose overlay 指向 bridge，并调整已有逐块/整批超时；旧平台源码与私有环境文件保留原样。
- [x] 重试原两份已附加文档的解析和预读，复用原 documentId/version，不再次上传已 resolved 补件动作。医院完成提取；LED 保留评分分类不足的部分结果，恢复条款和报告已落库。
- [x] 完整测试、独立审查、真实 Codex JSON/普通内容调用及预读结果、主目录部署与状态记录。OpenBid 218 项通过，消费者最终 207 项聚焦通过；回放保持 41 份模型请求不增长。
- [x] 两份预读报告生成飞书在线文档，存公司专用目录，逐行回读正文无缺失。
- [x] 补授 docs:permission.member:retrieve，读回指定群阅读权限后自动更新原项目卡的报告链接；两卡真实回读包含对应飞书文档链接。
- [x] 飞书回调订阅经新版应用发布生效；收到员工真实翻页事件，LED 原卡第 3/12 页回读一致，用户确认正常。
- [x] 群卡和飞书报告增加一眼判标摘要：资格结论、商务判断、一票否决、核心参数、前三风险和三项行动；原两卡及两文档真实回读，后续报告自动保留详细证据附录。

模型实际在云端，复用当前授权登录。新调用只接收本任务需要的内容，保持来源引用和未知事实占位；招标原文是待分析数据，不能作为工具或权限指令。公司归属、资格、人员、报价和投标选择仍由原流程门禁管理。运行记录不保存登录凭证，不修改执行策略或提取应用内部令牌。
